FROM python:3.12-slim

WORKDIR /app
COPY pyproject.toml README.md /app/
COPY src /app/src

RUN pip install --no-cache-dir .

ENV SARA_HOST=0.0.0.0
ENV SARA_PORT=8090

EXPOSE 8090

CMD ["python", "-m", "sara.service.main"]
